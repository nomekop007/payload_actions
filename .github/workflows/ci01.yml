  name: First workflow

  on: [push]

  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v2

        - name: hello codelies!
          run: echo "hello workflow!!!! from ${{ github.workflow }} triggered by the ${{ github.event_name }} done by ${{ github.actor }}"
          
        - name: More and more
          env:
              JOB_CONTEXT : ${{ toJson(job) }}
          run: |
            echo "job context: $JOB_CONTEXT"
            echo "Done!"
